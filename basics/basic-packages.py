import numpy as np      # essential array powertool for mathematics and data analytics
import pandas as pd     # 'panel-data', novel support for dataframes in data analytics
import copy             # deep copies manipulation

import collections      # contains better data structures, such as dict with explicit types
import itertools        # for iterations, use this when comfy with list comprehensions and generators
import csv
import math             # basic math functions
import re               # for regex
import string
import tempfile
import unittest

# added some text to see if changing nodejs version would affect nvim performance. 
